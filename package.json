{
  "name": "nextjs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild index.js --bundle > app.js",
    "predeploy": "npm run build",
    "deploy": "npm run build",
    "prettier": "prettier --write *.js --ignore-path app.js"
  },
  "dependencies": {
    "@hebcal/rest-api": "^3.8.4",
    "bootstrap": "^5.1.3",
    "date-fns": "^2.25.0",
    "esbuild": "^0.13.9",
    "prettier": "^2.4.1",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "zipcode-to-timezone": "^0.0.9"
  }
}
